<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <div>
        {% for institutes in institute %}
            <h2>title: {{ institute.id }}</h2>
            <h2>price: {{ institute.title }}</h2>
        {% else %}
            No activities have been found.
        {% endfor %}

    </div>
</body>

</html>