<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <div>
        {% for user in users %}
            <h2>title: {{ user.email }}</h2>
            <h2>price: {{ user.name }}</h2>
            <h2>price_month: {{ user.is_admin }}</h2>
        {% else %}
            No activities have been found.
        {% endfor %}

    </div>
</body>

</html>