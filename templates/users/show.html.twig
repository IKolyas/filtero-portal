<div>
    <h2>Имя:  {{ user.first_name }}</h2>
    <h2>Фамилия:  {{ user.last_name }}</h2>
    <h2>Email:  {{ user.email }}</h2>
    <h2>Login:  {{ user.login }}</h2>
    {% if user.fullname %}
        <h2>Полное имя:  {{ user.fullname }}</h2>
    {% endif %}
</div>