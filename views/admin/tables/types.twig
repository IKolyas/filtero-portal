<div class="activity_list">
    <table class="table table-striped table-hover mt-5">
        <thead>
        <tr>
            <th scope="col">#</th>
            <th class="col-10" scope="col">Название <i class="fa-solid fa-arrow-down"></i></th>
            <th scope="col">Действия</th>
        </tr>
        </thead>
        <tbody>
        {% for type in types %}
            <tr>
                <th scope="row">{{ type.id }}</th>
                <td class="js-types-content">{{ type.title }}</td>
                <td>
                    <a href="#" class="js-types-link__edit" data-id="{{ type.id }}"><i class="fa-solid fa-pencil text-success mx-2 activity_action_btn"></i></a>
                    <a href="/admin/deleteTypes?{{ type.id }}"><i class="fa-solid fa-trash-can text-danger mx-2 activity_action_btn"></i></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <th colspan="10" class="text-center">Список активностей пуст.</th>
            </tr>
        {% endfor %}
        </tbody>
    </table>

</div>
