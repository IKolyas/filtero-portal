<div class="activity_list">
    <table class="table table-striped table-hover mt-5">
        <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Имя <i class="fa-solid fa-arrow-down"></i></th>
            <th scope="col">Фамилия <i class="fa-solid fa-arrow-down"></i></th>
            <th scope="col">Логин <i class="fa-solid fa-arrow-down"></i></th>
            <th scope="col">e-mail <i class="fa-solid fa-arrow-down"></i></th>
            <th scope="col">Администратор <i class="fa-solid fa-arrow-down"></i></th>
            <th scope="col">Действия</th>
        </tr>
        </thead>
        <tbody>
        {% for useritem in user %}
            <tr>
                <th scope="row" data-field="id">{{ useritem.id }}</th>
                <td data-field="first_name">{{ useritem.first_name }}</td>
                <td data-field="last_name">{{ useritem.last_name }}</td>
                <td data-field="login">{{ useritem.login }}</td>
                <td data-field="email">{{ useritem.email }}</td>
                <td data-field="is_admin">{{ useritem.is_admin }}</td>
                <td>
                   <i class="fa-solid fa-pencil text-success mx-2 js-types-link__edit activity_action_btn"></i>
                    <a href="/admin/deleteUser?{{ useritem.id }}" onclick="return confirm('Вы уверенны что хотите удалить?')"><i class="fa-solid fa-trash-can text-danger mx-2 activity_action_btn"></i></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <th colspan="10" class="text-center">Список активностей пуст.</th>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
