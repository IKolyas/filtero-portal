<div class="card_list mb-2">
    <div class="container loader">
        <div id="spinner">
            <div class="spinner-border spinner-border-md text-primary spinner_circle" role="status">
                <span class="sr-only"></span>
            </div>
        </div>
    </div>
    {% for activity in activities %}
        {% include 'activities/item_mobile.twig' %}
    {% else %}
        <div class="h-100 d-flex align-items-center justify-content-center">
            <h2>Список активностей пуст</h2>
        </div>
    {% endfor %}
</div>